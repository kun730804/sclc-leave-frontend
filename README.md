SCLC Leave Frontend (static)

- index.html -> login.html
- login.html -> app.html (calendar) after successful login
- app.html -> shows CalendarEvents via Worker /api/events

Make sure api.js BASE points to your Worker URL.
